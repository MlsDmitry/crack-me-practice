/// <reference types="frida-gum" />
declare class ReferenceManager {
    private readonly references;
    constructor();
    add(ref: NativePointer): void;
    release(ref: NativePointer): void;
}
export { ReferenceManager };
